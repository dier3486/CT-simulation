function cfg = readcfgfile(cfgfile)

if exist(cfgfile, 'file')
    [~, ~, cfgext] = fileparts(cfgfile);
    switch cfgext
        case '.xml'
            cfg = xml2struct(cfgfile);
        case '.json'
            cfg = jsonread(cfgfile);
        case '.mat'
            cfg = load
else
    1;
end