function [dataflow, prmflow, status] = reconnode_beamhardenprepare(dataflow, prmflow, status)
% corr node, beamharden correction prepare
% [dataflow, prmflow, status] = reconnode_beamhardenprepare(dataflow, prmflow, status);

[dataflow, prmflow, status] = reconnode_nonlinearprepare(dataflow, prmflow, status);

end