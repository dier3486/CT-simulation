function [dataflow, prmflow, status] = reconnode_helicalazirebinprepare(dataflow, prmflow, status)
% prepare node, prepare of rebin
% [dataflow, prmflow, status] = reconnode_helicalazirebinprepare(dataflow, prmflow, status);

% rebin prepre
[dataflow, prmflow, status] = reconnode_helicalrebinprepare(dataflow, prmflow, status);

end