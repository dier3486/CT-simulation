function y = iterinvpolyval(p, x, r, n)
% return y satisfy ((((y*p(1)+1)*y*p(2)+1)*y*p(3)+1)*...)y*p(n) = x
% y = iterinvpolyval(p, x, r, n);

if nargin<3
    r = [min(x(:)).*0.8 max(x(:)).*1.2];
end

end